import 'package:flutter/material.dart';
class demoinfo extends StatelessWidget {
  const demoinfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      // width: 250,
      // height: 250,
      // color: Colors.redAccent,


    );
  }
}
